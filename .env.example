# Copy this file to .env and fill it in (keep your .env private).
# Loaded automatically by app.py, check_setup.py and setup_gpu.sh.

# HuggingFace token — authenticates model downloads (silences the
# "unauthenticated requests" warning, higher rate limits, faster downloads).
# Create one at https://huggingface.co/settings/tokens — "Read" access is enough.
HF_TOKEN=

# Optional overrides:
# PORT=8000            # app port (default 8000)
# MANGA_UPSCALE=1      # OFF by default. Set 1 to upscale tiny raws before the
#                      #   pipeline. With MangaJaNai installed this is FAITHFUL
#                      #   (sharpens, de-artifacts, doesn't redraw); the
#                      #   Real-ESRGAN fallback is more aggressive on the art.
# MANGAJANAI_DIR=models/mangajanai   # MangaJaNai model set (./setup_gpu.sh --mangajanai)
# BUBBLE_IMGSZ=2048    # bubble-detection resolution (default auto, ≤2048). Higher
#                      #   = catches more small/dense balloons.
# BUBBLE_CONF=0.15     # bubble-detection confidence (lower = higher recall)
# UPSCALE_MODEL=       # force a specific upscale model file (any spandrel model)
# TEXT_SEG_MODEL=      # custom path to comic-text-detector weights
