fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
jinja2>=3.1.0
anthropic>=0.39.0
httpx>=0.27.0
# Pinned <4.12: opencv 4.12+ requires numpy>=2, but simple-lama-inpainting
# (and the rest of the GPU stack here) need numpy<2 — without this cap pip
# flip-flops numpy 1.26<->2.x every install. Keep both on numpy 1.26.
opencv-python-headless>=4.10.0,<4.12.0
# Capped <11 for the same reason as numpy: simple-lama-inpainting needs
# pillow<11, and a stray `pip install rembg` was seen upgrading pillow to 12
# (and numpy to 2.5) right over it. rembg's metadata ASKS for numpy>=2.3 and
# pillow>=12 but runs fine on these pins — verified by inference — so pip's
# red "incompatible" grumble about rembg can be ignored.
Pillow>=10.0.0,<11.0.0
numpy>=1.26.0,<2.0.0
aiofiles>=24.0.0
# Used to read a font's glyph table so special characters (♪ ♫ ― …) fall back
# to a font that has them instead of rendering as boxes.
fonttools>=4.22.0
# Right-to-left output (Arabic, etc.): when Pillow is built WITH libraqm it
# shapes + reorders natively and these go unused. They are the portable
# fallback for Pillow builds WITHOUT raqm — pre-shape Arabic into joined,
# visually-ordered glyphs so it isn't drawn disconnected and backwards.
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
# The page cut-out ("✂ Cut out the page"): U2-Net traces the sheet's real
# outline where the classical cues can only draw a hull around it. The model
# (~176MB) downloads itself on first use. Both run fine on CPU; without them
# the cut-out still works, just with a rougher edge.
rembg>=2.0.60
onnxruntime>=1.17.0
